server:
  host: "0.0.0.0"   # Écoute sur toutes les interfaces (WiFi + RJ45)
  port: 8000

database:
  path: "./bletrack.db"

jwt:
  secret: "bletrack-secret-change-in-production"
  algorithm: "HS256"
  expire_minutes: 30

mqtt:
  broker: "localhost"   # Broker tourne sur ce Pi (RJ45 10.0.1.10)
  port: 1883
  topic_prefix: "bletrack"
  enabled: true

redis:
  host: "localhost"
  port: 6379
  ttl: 30
  enabled: false

gateways:
  - id: "gw1"
    name: "Gateway Pi4"
    ip: "10.0.1.10"
    floor: 1
    x: 3.0
    y: 5.0

ble:
  rssi_1m: -59
  path_loss: 2.5
  buffer_size: 4
  tag_timeout: 30

positioning:
  save_interval: 5
  history_retention_days: 30
  simulation_mode: false

building:
  width: 30.0
  height: 10.0
  floors: 10
